# ``XestiToolsCLI``

@Metadata {
    @PageColor(blue)
}

Command-line interface tools built on XestiTools.

## Overview

The XestiToolsCLI framework provides utilities for building command-line
interface programs, including command runners and extensions for working with
exit codes, file paths, and URLs.
